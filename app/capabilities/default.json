{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Permissions for the local settings window",
  "windows": ["main"],
  "permissions": ["core:default"]
}

