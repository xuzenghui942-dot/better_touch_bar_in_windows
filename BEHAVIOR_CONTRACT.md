# 行为契约

此文件固定 Rust 重写必须保持的产品行为。行为基准是工作区中当前
`ThreeFiguerDrag` C# 源码，而不是旧 Rust 重写版。

## 默认设置

| 设置 | 默认值 |
| --- | --- |
| 三指拖动 | 开启 |
| 鼠标键 | 左键 |
| 允许离开后继续 | 开启 |
| 释放延迟 | 500 ms |
| 每设备指针移动 | 开启 |
| 每设备速度 | 30 |
| 每设备加速度 | 10 |
| 指针移动平均 | 1 |
| 单帧最大移动 | 0（无限制） |
| 开始阈值 | 100 |
| 结束阈值 | 10 |
| 开机启动 | 关闭 |
| 管理员权限运行 | 开启 |
| 记录日志 | 关闭 |

结束阈值不得高于开始阈值。释放延迟最大为 2,000 ms，移动平均范围为
1–10。

## 输入与拖动

1. 仅注册 HID Usage Page `0x0D`、Usage `0x05` 的精确式触摸板。
2. 组合设备拆分上报的接触点，并按接触点 ID 去重。
3. 输入间隔超过 40 ms 视为手指曾离开；新接触点经过 40 ms 隔离期后才参与距离计算。
4. 使用同一帧中移动距离最长的接触点作为指针位移。
5. 阈值距离乘以 `速度 / 60`；指针位移乘以 `速度 / 120`，再应用原版的 sigmoid 加速度曲线。
6. 只有最初确认的三根手指可开始拖动。拖动期间允许暂时剩余两根手指。
7. 只有已保存的设备配置且该设备启用指针移动时，才移动指针并刷新释放计时器。
8. 允许继续拖动时，释放计时为 `max(设置延迟, 40 ms)`；否则固定为 40 ms。
9. 应用退出时若仍处于拖动状态，必须发送与当前设置相符的鼠标键抬起事件。

## 桌面行为

- 关闭设置窗口只隐藏窗口，后台服务继续运行。
- 托盘左键或“打开设置”显示并聚焦设置窗口；“退出应用”停止输入服务并退出。
- 第二个实例只激活现有设置窗口。
- 开机启动可使用当前用户 Run 注册表项，或使用最高权限的计划任务。
- 日志只在明确开启时记录，内存中最多保留最近 10,000 行。
- 设置、启动项、计划任务和日志文件均使用独立名称，不接触原版数据。

## 明确不包含

- 任何四指操作
- 触摸板边缘操作
- 系统音频或显示设置控制
- 三指轻点、滚动、缩放或其他额外手势
- 自动更新器
